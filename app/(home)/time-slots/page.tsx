import TimeSlotsPicker from "./_components/time-slots-picker";

const TimeSlots = () => {
    return (
        <div className="min-h-screen flex items-center justify-center">
            <TimeSlotsPicker />
        </div>
    );
};

export default TimeSlots;
